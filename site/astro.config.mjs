// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: replace once the GitHub Pages / custom domain target is confirmed
  // (see "Open items" in PLAN.md). Needed for canonical URLs, RSS, sitemap.
  site: 'https://javedjamil.example',
});
